server:
  port: 8000

grpc:
  port: 6565

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nab_customer?useUnicode=true&amp;characterEncoding=UTF-8
    username: root
    password: 123456789
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: nab_customer
        enable_lazy_load_no_trans: true
        format_sql: false
        temp:
          use_jdbc_metadata_defaults: false
    show-sql: true
