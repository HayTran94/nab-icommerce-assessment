server:
  port: 8200

grpc:
  port: 8201

spring:
  application:
    name: product-query-service
  elasticsearch:
    rest:
      uris: localhost:9200

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
    enabled: true
  instance:
    preferIpAddress: true